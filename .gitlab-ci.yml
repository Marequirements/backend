stages:
  - build
  - test 
  - deploy

build:
  stage: build
  image: golang:latest
  script:
    - go build -o app .

test:
  stage: test
  script:
    - go test ./..

deploy:
  stage: deploy
  image: docker:latest
  script:
    - docker build -t app .
    - docker run app

