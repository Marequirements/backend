stages:
  - build
  - test 
  - deploy

build:
  stage: build
  image: golang:latest
  script:
    - go build -o app .

test:
  stage: test
  image: golang:latest
  script:
    - ./..

deploy:
  stage: deploy
  image: docker:latest
    - docker run dockerfile

