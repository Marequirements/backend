stages:
  - build
  - test 
  - deploy

build:
  stage: build
  image: golang:latest
  script:
    - go build -o app .
    - sudo docker build -t app .

test:
  stage: test
  script:
    - go test ./..

deploy:
  stage: deploy
  script:
    - sudo docker run app

